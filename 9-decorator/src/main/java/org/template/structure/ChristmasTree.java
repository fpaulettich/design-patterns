package org.template.structure;

/**
 * Component, interface that defines the objects to which responsibilities could be dynamically added.
 */
public interface ChristmasTree {

    String decorate();

}
